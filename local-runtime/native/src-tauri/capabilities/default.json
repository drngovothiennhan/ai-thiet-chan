{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the local runtime shell",
  "windows": ["main"],
  "permissions": ["core:default"]
}
